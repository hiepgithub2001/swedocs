# Source books

Drop a PDF you own here and it can be read locally while writing a
distillation under `content/`. Nothing in this directory is committed —
`.gitignore` keeps every file out of the repository, and no passage of a
source book is ever reproduced in the built output.
